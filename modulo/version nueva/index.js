// Es recomendable usar constantes para no alterar el contenido
import { suma, resta, creador, alumnos } from './matematica.js';

console.log(suma(10, 5));         // 15
console.log(resta(10, 5));        // 5
console.log(creador);             // "Nicolas"
console.log(alumnos.length);     // 5