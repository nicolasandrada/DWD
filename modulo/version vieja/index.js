// Es recomendable usar constantes para no alterar el contenido
const m = require('./matematica')

// Se llama primero a la constante y luego a su contenido
console.log( m.suma(10,4) )
console.log( m.creador )